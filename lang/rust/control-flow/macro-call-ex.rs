fn main() {
    // the macro and function call both return a empty tumple by default
    // just applicable there is'nt a return value signature
    let y = println!("that is the result of macro call?");

    println!("the value of y is: {:?}", y);
}
